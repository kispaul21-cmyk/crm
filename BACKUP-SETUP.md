# 📦 Инструкция по настройке автоматического бэкапа CRM

## Что делает система бэкапа:

✅ Проверяет проект на ошибки (npm run lint)  
✅ Создает копию проекта в отдельную папку с временной меткой  
✅ Делает Git коммит изменений  
✅ Хранит последние 5 версий (старые автоматически удаляются)  
✅ Запускается каждый час автоматически  

## Структура бэкапов:

```
d:\Projects\crm-backups\
├── crm-backup-2025-12-02_12-00\
├── crm-backup-2025-12-02_13-00\
├── crm-backup-2025-12-02_14-00\
├── crm-backup-2025-12-02_15-00\
└── crm-backup-2025-12-02_16-00  (последний)
```

## Настройка (2 способа):

### Способ 1: Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

1. **Запустите PowerShell от имени администратора**
   - Win + X → "Windows PowerShell (Администратор)"

2. **Выполните команду:**
```powershell
cd "d:\Projects\crm"
.\setup-auto-backup.ps1
```

3. **Готово!** Задача создана и будет запускаться каждый час.

---

### Способ 2: Ручная настройка через планировщик задач

1. **Откройте планировщик задач**
   - Win + R → `taskschd.msc` → Enter

2. **Создайте новую задачу**
   - Действия → Создать задачу

3. **Вкладка "Общие":**
   - Имя: `CRM Auto Backup`
   - Описание: `Автоматический бэкап CRM проекта каждый час`
   - ✅ Выполнять с наивысшими правами

4. **Вкладка "Триггеры":**
   - Создать → Повторять задачу каждые: `1 час`
   - Начать: `Сегодня` в любое время
   - ✅ Включено

5. **Вкладка "Действия":**
   - Программа: `powershell.exe`
   - Аргументы: `-NoProfile -ExecutionPolicy Bypass -File "d:\Projects\crm\auto-backup.ps1"`

6. **Вкладка "Условия":**
   - ❌ Запускать только при питании от сети (снять галочку)

7. **Сохраните задачу**

---

## Проверка работы:

### Тестовый запуск (вручную):
```powershell
cd "d:\Projects\crm"
.\auto-backup.ps1
```

### Проверка созданных бэкапов:
```powershell
explorer "d:\Projects\crm-backups"
```

### Проверка задачи в планировщике:
```powershell
Get-ScheduledTask -TaskName "CRM Auto Backup"
```

### Просмотр истории запусков:
```powershell
Get-ScheduledTask -TaskName "CRM Auto Backup" | Get-ScheduledTaskInfo
```

---

## Управление:

### Отключить автобэкап:
```powershell
Disable-ScheduledTask -TaskName "CRM Auto Backup"
```

### Включить автобэкап:
```powershell
Enable-ScheduledTask -TaskName "CRM Auto Backup"
```

### Удалить автобэкап:
```powershell
Unregister-ScheduledTask -TaskName "CRM Auto Backup" -Confirm:$false
```

### Запустить бэкап прямо сейчас:
```powershell
Start-ScheduledTask -TaskName "CRM Auto Backup"
```

---

## Настройки (можно изменить в auto-backup.ps1):

- `$maxBackups = 5` - количество хранимых версий (3-10)
- Интервал: 1 час (можно изменить в планировщике)
- Папка бэкапов: `d:\Projects\crm-backups`

---

## Что НЕ копируется (для экономии места):

❌ node_modules (большая папка с зависимостями)  
❌ .git (история Git хранится отдельно)  
❌ dist/build (собранные файлы)  
❌ .vscode (настройки редактора)  

---

## Восстановление из бэкапа:

1. Откройте папку: `d:\Projects\crm-backups`
2. Выберите нужную версию (по дате/времени)
3. Скопируйте содержимое в `d:\Projects\crm`
4. Установите зависимости: `npm install`
5. Запустите проект: `npm run dev`

---

## Логи и отладка:

Если бэкап не работает, проверьте:
1. Права администратора
2. Путь к проекту правильный
3. npm установлен и доступен
4. Достаточно места на диске

Логи задачи: Планировщик задач → История
